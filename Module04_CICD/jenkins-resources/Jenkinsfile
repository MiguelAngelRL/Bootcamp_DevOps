pipeline {
    agent any

    stages {
        stage('Java Compile') {
            steps {
                dir('./calculator'){
                    sh 'gradlew compileJava'
                }
            }
        }

        stage('Java Unit Tests') {
            steps {
                dir('./calculator'){
                    sh 'gradlew test'
                }
            }
        }
    }
}